{{! Navigation Docs: https://themes.ghost.org/docs/navigation }}

<nav class="site-nav">
	<ul class="primary-nav">
		{{#foreach navigation}}
		<li{{#if current}} class="current"{{/if}}>
			<a href="{{url absolute="true"}}">{{label}}</a>{{^if @last}}&nbsp;&#124{{/if}}
		</li>
		{{/foreach}}
	</ul>
</nav>
